<!--
  PLACEHOLDER — the actual setup prompt will be added here later.

  Purpose: this file holds the single message she will paste into her
  Claude Code on first run. When she pastes it, Claude will:
    1. Strip the GitHub / contributor / open-source scaffolding she
       does not need (this is a local-only fork for her use).
    2. Walk her through the onboarding (CV, profile, target roles)
       in plain language — no mention of git, branches, PRs, or
       commands she has to type.
    3. Lock the workflow to: paste a job-description PDF → get an
       evaluation. If she pastes a link, Claude must politely tell
       her the link will not work and ask her to save the page as a
       PDF from Chrome (File → Print → Save as PDF) and paste that
       instead.

  Until the real prompt is written, this file is intentionally a
  placeholder so the slot exists in the codebase she will receive.
-->

# Setup prompt — TO BE WRITTEN

(Placeholder. Real content coming later.)
