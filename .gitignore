# Personal data (user fills these)
cv.md
data/applications.md
data/pipeline.md
data/scan-history.tsv
reports/*.md
!reports/.gitkeep
!output/.gitkeep
batch/logs/*
!batch/logs/.gitkeep
batch/batch-state.tsv
batch/batch-input.tsv
batch/tracker-additions/**/*.tsv
!batch/tracker-additions/.gitkeep
jds/*
!jds/.gitkeep

# User config and customization (never auto-updated)
# NOTE: portals.yml was removed from this list so the Render web service can
# run scan.mjs at runtime. portals.yml contains no secrets — only company
# names, public careers URLs, and title filters.
# NOTE: modes/_profile.md is tracked so personal delivery rules sync to cloud
# Claude sessions. config/profile.yml stays ignored (contains comp data).
config/profile.yml
.update-dismissed
.update-lock

# Secrets — never commit (use .env.example as template)
.env
.env.*
!.env.example

# Generated
.resolved-prompt-*
node_modules/
bun.lock

# OS
.DS_Store
*.mov
*.mp4

# Claude Code local
.claude/settings.local.json
.claude/memory/
career-dashboard
package-lock.json

__pycache__/
*.pyc
